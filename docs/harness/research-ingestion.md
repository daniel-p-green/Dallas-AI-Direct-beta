# Web Research Ingestion Policy

1. Try `https://markdown.new/<url>` first.
2. Fall back to normal fetch if incomplete.
3. Use browser rendering for JS-heavy pages.
4. Save normalized markdown under `docs/harness/research/`.
